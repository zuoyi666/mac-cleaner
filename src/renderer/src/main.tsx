import React from 'react'
import ReactDOM from 'react-dom/client'
import { MacCleanerApp } from './MacCleanerApp'
import './styles.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MacCleanerApp />
  </React.StrictMode>
)
